<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- Only the downloaded update APK is ever shared, and only with the
         system package installer. -->
    <cache-path name="updates" path="." />
</paths>
